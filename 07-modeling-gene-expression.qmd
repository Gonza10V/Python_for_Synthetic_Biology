# Modeling Gene Expression

## Draft focus

Bridge simple programming to dynamical systems and quantitative reasoning.

## Starter example

```{python}
import numpy as np

time = np.linspace(0, 10, 6)
production = 2.0
loss = 0.3
protein = production / loss * (1 - np.exp(-loss * time))
protein.round(3)
```
